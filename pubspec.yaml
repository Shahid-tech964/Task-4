name: task_4
description: "Little Explorer - A kid learning app for ages 3-5."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  camera: ^0.11.1
  google_mlkit_image_labeling: ^0.14.1
  provider: ^6.1.5
  flutter_tts: ^4.2.3
  confetti: ^0.8.0
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
